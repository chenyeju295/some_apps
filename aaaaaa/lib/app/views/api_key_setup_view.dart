import 'package:flutter/material.dart';

class ApiKeySetupView extends StatelessWidget {
  const ApiKeySetupView({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('API Setup'),
      ),
      body: const Center(
        child: Text('API Key Setup - To be implemented'),
      ),
    );
  }
}
